name = "private-podcast"
main = "worker.js"
compatibility_date = "2024-01-01"

# Bind your private R2 bucket to the variable name BUCKET
# Replace "podcast-files" with your actual bucket name
[[r2_buckets]]
binding = "BUCKET"
bucket_name = "podcast-files"
